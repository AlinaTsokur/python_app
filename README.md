# VANTA — Система анализа криптовалютных данных

## 📋 Обзор

VANTA — это Streamlit-приложение для анализа криптовалютных данных с упором на:
- Парсинг данных с Coinglass
- Расчёт производных метрик (CVD, OI, геометрия свечей)
- Обучение и применение паттерн-майнинг моделей
- Визуализация и генерация отчётов

---

## 📁 Структура проекта

```
python_app/
├── app.py                 # Главный файл приложения (~175 строк)
├── styles.py              # CSS стили для UI
├── requirements.txt       # Python зависимости
├── Procfile              # Конфигурация Railway (деплой)
│
├── core/                  # 🔧 Ядро системы (бизнес-логика)
│   ├── parsing_engine.py     # Парсинг сырого текста → метрики
│   ├── batch_parser.py       # Batch-парсинг с метками (Лаборатория)
│   ├── diver_engine.py       # Анализ дивергенций
│   ├── levels_engine.py      # Расчёт уровней поддержки/сопротивления
│   ├── report_generator.py   # Генерация X-Ray и композитных отчётов
│   ├── pipeline_processor.py # Обработка пайплайнов
│   └── db_manager.py         # Менеджер базы данных (Supabase)
│
├── ui/                    # 🎨 UI компоненты
│   └── tabs/                 # Вкладки приложения
│       ├── tab_reports.py       # Вкладка "Отчёты"
│       ├── tab_candles.py       # Вкладка "Свечи"
│       ├── tab_diver.py         # Вкладка "Дивер" (дивергенции)
│       ├── tab_levels.py        # Вкладка "Уровни"
│       ├── tab_lab.py           # Вкладка "Лаборатория"
│       └── tab_training.py      # Вкладка "Обучение"
│
├── offline/               # 🎓 Offline-обучение модели
│   ├── stage1_loader.py      # Шаг 1: Загрузка данных из БД
│   ├── stage2_features.py    # Шаг 2: Генерация признаков
│   ├── stage3_bins.py        # Шаг 3: Квантование (bins)
│   ├── stage4_rules.py       # Шаг 4: Поиск паттернов (mining)
│   ├── stage5_bins_stats.py  # Шаг 5: Статистика bins
│   ├── stage6_mine_stats.py  # Шаг 6: Статистика правил
│   ├── tokenizer.py          # Токенизация признаков
│   ├── stats_calc.py         # Калькулятор статистики
│   └── data/                 # Локальные данные (JSON)
│
├── online/                # 🔴 Online-детекция (в разработке)
│
├── assets/                # Ресурсы (иконки, изображения)
│
└── .streamlit/            # Конфигурация Streamlit
    └── config.toml
```

---

## 🔧 Модули ядра (`core/`)

### `parsing_engine.py`
Основной парсер сырых данных свечей.

**Функции:**
- `parse_value_raw(val_str)` — парсит строки с K/M/B суффиксами
- `parse_raw_input(text)` — извлекает OHLC, OI, Volume из текста
- `calculate_metrics(raw_data, config)` — рассчитывает производные метрики:
  - Геометрия: `body_pct`, `clv_pct`, `upper/lower_tail_pct`
  - CVD: `cvd_pct`, `cvd_sign`, `price_vs_delta`
  - OI: `doi_pct`, `oipos`, `oi_path`, `oi_set/counter/unload`
  - Ликвидации: `liq_share_pct`, `limb_pct`, `liq_squeeze`
  - Паттерны: `dominant_reject`

---

### `batch_parser.py`
Парсер для batch-загрузки данных с метками (Weak/Medium/Strong Up/Down).

**Функции:**
- `parse_batch_with_labels(text, config)` — разбивает текст на сегменты
- `calculate_stats_agg(candles)` — агрегированная статистика сегмента
- `save_batch_transactionally(supabase, segments, candles)` — транзакционная запись

---

### `diver_engine.py`
Анализ дивергенций между ценой и индикаторами.

---

### `levels_engine.py`
Автоматический расчёт уровней поддержки/сопротивления.

---

### `report_generator.py`
Генерация текстовых отчётов.

**Функции:**
- `generate_xray(d)` — X-Ray отчёт по одной свече
- `generate_composite(candles_list)` — композитный отчёт по нескольким биржам

---

## 🎨 UI Вкладки (`ui/tabs/`)

| Вкладка | Файл | Описание |
|---------|------|----------|
| Отчёты | `tab_reports.py` | Парсинг свечей, X-Ray отчёты, сохранение в БД |
| Свечи | `tab_candles.py` | Просмотр сохранённых свечей из БД |
| Дивер | `tab_diver.py` | Анализ дивергенций, мультибиржевой ввод |
| Уровни | `tab_levels.py` | Расчёт и визуализация уровней |
| Лаборатория | `tab_lab.py` | Batch-загрузка обучающих данных с метками |
| Обучение | `tab_training.py` | 6-этапный конвейер обучения модели |

---

## 🎓 Offline-обучение (`offline/`)

6-этапный конвейер обучения модели паттерн-майнинга:

| Stage | Файл | Описание |
|-------|------|----------|
| 1 | `stage1_loader.py` | Загрузка сегментов из Supabase |
| 2 | `stage2_features.py` | Симуляция и генерация признаков |
| 3 | `stage3_bins.py` | Построение квантилей (bins) |
| 4 | `stage4_rules.py` | Поиск паттернов (frequent itemsets) |
| 5 | `stage5_bins_stats.py` | Статистика по bins |
| 6 | `stage6_mine_stats.py` | Финальная статистика правил |

**Вспомогательные:**
- `tokenizer.py` — преобразование признаков в токены
- `stats_calc.py` — расчёт статистики

---

## 🗄️ База данных (Supabase)

### Таблицы:
- `candles` — свечи с метриками
- `segments` — обучающие сегменты с метками
- `configurations` — конфигурации (пороги, коэффициенты)
- `training_artifacts` — артефакты обучения (bins, rules)

---

## 🚀 Запуск

### Локально:
```bash
cd python_app
source venv/bin/activate
streamlit run app.py
```

### Через лаунчер:
```bash
./start_app.command
```

---

## 📦 Зависимости

См. `requirements.txt`:
- `streamlit` — UI фреймворк
- `pandas` — работа с данными
- `supabase` — клиент БД
- `mlxtend` — frequent itemset mining

---

## 🔐 Конфигурация

Переменные окружения (`.streamlit/secrets.toml`):
```toml
SUPABASE_URL = "https://xxx.supabase.co"
SUPABASE_KEY = "eyJ..."
```

---

## 📝 Версия

**VANTA v2.1** — Январь 2026
